<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:label="UE Quest ModLoader (LSPosed)"
        android:allowBackup="false">

        <!-- Marks this APK as an Xposed/LSPosed module -->
        <meta-data
            android:name="xposedmodule"
            android:value="true"/>

        <meta-data
            android:name="xposeddescription"
            android:value="Loads libmodloader.so into RE4 VR and Pinball FX VR — no smali patching required"/>

        <!-- Minimum Xposed API version (82 = Xposed 3.0+, works with LSPosed) -->
        <meta-data
            android:name="xposedminversion"
            android:value="82"/>

    </application>

</manifest>
